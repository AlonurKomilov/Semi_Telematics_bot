# [rate-limiter] pulls aiolimiter — required since the Application
# builders attach AIORateLimiter (it raises at startup without it).
python-telegram-bot[rate-limiter]==21.6
aiohttp==3.10.11
python-dotenv==1.0.1
apscheduler==3.10.4
reportlab==4.4.10
# Storage is Postgres-only (cutover 2026-05-08).  aiosqlite is not
# installed in prod or dev — ``adapters/storage/core.py`` hard-requires
# ``DATABASE_URL`` and the test suite spins up a ``testcontainers``
# Postgres instance (no SQLite fallback).
cachetools>=5.3
redis>=5.0
cryptography>=41.0
bcrypt>=4.0
staticmap>=0.5.7
google-generativeai>=0.8
google-api-python-client>=2.120
google-auth>=2.28
# OAuth Web Flow helper for per-account "Bring Your Own Drive" backend
# (adapters/storage/object_store_gdrive.py).  google-auth doesn't ship
# the Flow class — it lives in this separate package.
google-auth-oauthlib>=1.2
python-multipart>=0.0.9

# API server
fastapi>=0.115
uvicorn[standard]>=0.30
# Multi-worker production runner — fronts uvicorn workers in prod.
# `python run.py` still uses uvicorn directly for single-process dev.
gunicorn>=22.0
python-jose[cryptography]>=3.3
slowapi>=0.1.9

# Async job queue (Phase 3) — Redis-backed task queue used for cache
# pre-warming, async report generation, and any deferred heavy work.
# Worker process: `arq capabilities.jobs.worker.WorkerSettings`.
arq>=0.26

# Observability (Phase 6) — Prometheus /metrics endpoint + OpenTelemetry
# auto-instrumentation. All four are optional infra: when not installed,
# infra/observability.py degrades to a no-op so the app keeps running.
prometheus-client>=0.20
prometheus-fastapi-instrumentator>=7.0
opentelemetry-api>=1.27
opentelemetry-sdk>=1.27
opentelemetry-instrumentation-fastapi>=0.48b0
opentelemetry-instrumentation-aiohttp-client>=0.48b0
# Host resource sampling (capacity monitoring on the operator console).
psutil>=5.9
opentelemetry-instrumentation-asyncpg>=0.48b0
opentelemetry-instrumentation-redis>=0.48b0
opentelemetry-exporter-otlp-proto-http>=1.27

# Postgres driver — required (DATABASE_URL is mandatory).
asyncpg>=0.29

# Stripe billing (optional — only needed when BILLING_PROVIDER=stripe)
stripe>=8.0

# Testing
pytest>=8.0
pytest-asyncio>=0.23
pytest-cov>=5.0
pytest-xdist>=3.5
pytest-watch>=4.2
