"""``datatruck_drivers`` storage mixin (stub).

Will own the upsert + read paths for drivers synced from Datatruck.
Schema and methods land alongside the first sync writer in the next
phase.  Stub kept in place so the architecture (one file per
Datatruck resource) is enforced from day one.
"""

from __future__ import annotations
