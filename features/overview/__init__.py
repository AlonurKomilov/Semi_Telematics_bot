"""Overview — the dashboard landing aggregator (System tier).

A pure aggregator: it has no domain data of its own — router.py reads
the other features' services/warehouse and serves the landing-page
stats.  Frontend twin: interfaces/dashboard/src/features/overview/.
"""
