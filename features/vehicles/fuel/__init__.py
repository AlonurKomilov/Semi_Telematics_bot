"""Vehicle fuel — a component of the Vehicle feature (docs/FEATURES.md).

Import the submodule you need directly — this __init__ deliberately
re-exports NOTHING so importing one hub contribution never drags
another hub's machinery into the import graph.
"""
