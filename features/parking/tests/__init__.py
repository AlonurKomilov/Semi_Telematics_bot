"""Tests owned by features/parking.

A package, not a bare directory: pytest imports test modules by name,
and without __init__.py two same-named test files in different packages
collide at import time.  The top-level tests/ tree has carried an
__init__.py for exactly this reason.
"""
