"""Driver documents — a component of the Driver feature (docs/FEATURES.md).

alert.py owns the document-expiry alert source (CDL / medical / MVR…),
registered as ``driver_doc_expiry_check``.  Import submodules directly —
this __init__ deliberately re-exports nothing.
"""
