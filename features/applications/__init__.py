"""Driver Applications feature — public application intake + recruiter
review dashboard.

The recruiter *role* (capabilities/permissions) manages this *feature*;
the two are intentionally separate names.  Router co-located here per the
feature-centric layout; the DB mixin lives in
``adapters/storage/applications.py``.
"""
